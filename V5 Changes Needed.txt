-Qty should have default value of 1. add + and - button for fast and easy qty manipulations (+ top while - on the bottom at the right side of qty input box)
-Must add a feature for Commercial Invoice and Serial Product file matching as it should filter to the most recent file as there could be multiple pdf contain the same code "R5542651","R5542050" & "R5542050A" but the latest pdf downloaded was the one need to renamed and sorted
-Need to add google chrome tab like feature for multiple shipment. Under title header before INPUTS & OUTPUTS should be top bookmark style tab like chrome with "+" button to add another "virtual window" of shipment so user can work with multiple shipment while not losing previous data. Make it look like chrome tab with default name of "Shipment + nth tab" but change to Destination value upon receive destination value. Title should be highlighted blue like the output text
-clear all button should be move to the far right side of "OUTPUTS" text so that the button now clear individual "virtual window" instead.
-same like google chrome, there are "x" button for closing tab but make a popup confirmation with distinct color on the button border rim
